/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.ctfassets.net'], // Add the domain you are loading images from
    },
}

export default nextConfig
